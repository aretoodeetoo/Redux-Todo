import React from 'react';
import { connect } from 'react-redux';

const TodoList = props => {
    return (
        <div>
        <h1>Hello, World!</h1>
        </div>
    );
}

export default TodoList;